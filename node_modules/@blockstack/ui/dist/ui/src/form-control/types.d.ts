import * as React from 'react';
import { BoxProps } from '../box';
interface FormControlBase {
    children?: React.ReactNode;
    isInvalid?: boolean;
    isRequired?: boolean;
    isDisabled?: boolean;
    isReadOnly?: boolean;
}
export declare type FormControlProps = FormControlBase & BoxProps;
export {};
