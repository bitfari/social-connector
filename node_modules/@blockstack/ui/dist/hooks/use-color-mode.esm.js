import { useContext } from 'react';
import { ColorModeContext } from '../color-modes/index.esm.js';

var useColorMode = function useColorMode() {
  var _useContext = useContext(ColorModeContext),
      colorMode = _useContext.colorMode,
      toggleColorMode = _useContext.toggleColorMode;

  return {
    colorMode: colorMode,
    toggleColorMode: toggleColorMode
  };
};

export { useColorMode };
//# sourceMappingURL=use-color-mode.esm.js.map
